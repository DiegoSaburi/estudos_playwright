# Estudos Playwright

Testes automatizados em Python com Playwright e pytest.

## Instalação

```bash
python -m venv .venv
.venv\Scripts\activate
pip install pytest-playwright
playwright install
```

## Executar os testes

```bash
pytest
```
